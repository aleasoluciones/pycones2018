# -*- coding: utf-8 -*-

from mamba import description, context, it
from expects import expect


with description('nameday http client specs'):
    with context('foo'):
        with it('bar'):
            pass
